import type { ThemeDefinition } from "./types";

/**
 * Add durable course-specific themes here. The learner can experiment in Theme
 * Studio, export the resulting theme in a lesson return, and the tutor can
 * promote a successful theme into this array.
 */
export const courseThemes: ThemeDefinition[] = [];
